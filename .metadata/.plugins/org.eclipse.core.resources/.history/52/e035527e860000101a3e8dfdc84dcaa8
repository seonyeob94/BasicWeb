<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Insert title here</title>
</head>
<body>
<script>
  // 변수 호이스팅
  // 나중에 밑에서 변수가 선언 되더라도 내부적으로 끌어올리기로 실행한
  // var는 가능-undefined로 실행, let,const는 불가능-오류가 발생
  console.log("a=", a);
  
  a=10;
  
  //const a=20;
  //let a
  var a;
  
  //함수 호이스팅
  printValue(); // 함수호출
  
 /*  function printValue() {
	  //함수선언식 printValue는 함수의 이름
	alert("hellow~~"); */
	
  printValue = function(){
	  //함수표현식 : printValue는 function()을 대입받는 변수
      alert("hellow");
	} 	
}
</script>
</body>
</html>